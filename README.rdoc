== README

* Database initialization

Rename config/application.yml.example to config/application.yml
Fill username and password fields in config/application.yml with your data, then run:
rake db:create
rake db:migrate
rake db:seed

* How to run the test suite

rspec
